<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Include Spring Boot's default configuration -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- Define properties for different profiles -->
    <springProfile name="local,dev">
        <property name="LOG_LEVEL" value="DEBUG"/>
        <property name="LOG_FILE" value="logs/gymcrm-dev.log"/>
    </springProfile>
    
    <springProfile name="stg">
        <property name="LOG_LEVEL" value="INFO"/>
        <property name="LOG_FILE" value="logs/gymcrm-staging.log"/>
    </springProfile>
    
    <springProfile name="prod">
        <property name="LOG_LEVEL" value="INFO"/>
        <property name="LOG_FILE" value="logs/gymcrm-production.log"/>
    </springProfile>

    <!-- Console Appender with colored output for development -->
    <springProfile name="local,dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) %cyan([%thread]) %yellow(%logger{36}) - %green([%X{transactionId}]) %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- File Appender for all environments -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - [%X{transactionId}] [%X{userId}] %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Audit Log Appender -->
    <appender name="AUDIT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/gymcrm-audit.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - [%X{transactionId}] [%X{userId}] %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/gymcrm-audit.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>
            <maxHistory>90</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Performance Log Appender -->
    <appender name="PERFORMANCE_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/gymcrm-performance.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{ISO8601} [%thread] %-5level %logger{36} - [%X{transactionId}] %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/gymcrm-performance.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <!-- Only log performance-related messages -->
        <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
            <evaluator>
                <expression>return message.contains("REQUEST") || message.contains("PERFORMANCE");</expression>
            </evaluator>
            <OnMismatch>DENY</OnMismatch>
            <OnMatch>ACCEPT</OnMatch>
        </filter>
    </appender>

    <!-- Logger configurations -->
    
    <!-- Audit Service Logger - sends to both main log and audit log -->
    <logger name="com.gym.crm.service.AuditService" level="INFO" additivity="false">
        <appender-ref ref="FILE"/>
        <appender-ref ref="AUDIT_FILE"/>
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
    </logger>

    <!-- Performance Logging Filter - sends to performance log -->
    <logger name="com.gym.crm.config.PerformanceLoggingFilter" level="INFO" additivity="false">
        <appender-ref ref="FILE"/>
        <appender-ref ref="PERFORMANCE_FILE"/>
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
    </logger>

    <!-- Transaction Interceptor -->
    <logger name="com.gym.crm.config.TransactionInterceptor" level="INFO"/>

    <!-- Exception Handler -->
    <logger name="com.gym.crm.exception.GlobalExceptionHandler" level="WARN"/>

    <!-- Application loggers -->
    <logger name="com.gym.crm" level="${LOG_LEVEL}"/>
    
    <!-- Third-party loggers -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.eclipse.jetty" level="WARN"/>

    <!-- Root logger -->
    <root level="INFO">
        <springProfile name="local,dev">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="FILE"/>
    </root>

</configuration>


